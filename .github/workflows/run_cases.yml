name: Examples (Linux x86-64, Python 3.12)

on: push

# Full example suite on Python 3.12. Delegates to the reusable _run_cases workflow.
jobs:
  examples:
    uses: ./.github/workflows/run_cases_workflow.yaml
    with:
      python-version: '3.12'
      case-path: '.'
